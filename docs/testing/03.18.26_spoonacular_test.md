# Manual Test: Spoonacular Recipe Search API Connection

---

**Date:** 2026-03-18

**Name of the person performing the test:** Evan Stonestreet


**Test Steps:**

1. Open a terminal in the `backend/` directory of the Sophros project
2. Ensure the backend dependencies are installed by running `uv sync`
3. Ensure a valid Spoonacular API key is set in the environment as `SPOONACULAR_API_KEY`
4. Create or confirm the presence of a manual test file for Spoonacular recipe search connectivity
5. Run the manual test directly with pytest:
   `uv run pytest backend/tests/manual/test_spoonacular_connection.py -s`
6. Observe the test output in the terminal
7. Confirm that the request to Spoonacular’s recipe search endpoint completes successfully

**Expected results:**

- A manual pytest test for Spoonacular recipe search connectivity runs successfully when invoked directly
- The test reads `SPOONACULAR_API_KEY` successfully from the environment
- A request to Spoonacular’s `recipes/complexSearch` endpoint is made successfully
- The HTTP response status is `200`
- The returned JSON contains a `results` field
- The `results` field is a list, confirming a valid recipe search response structure

**Actual results:**

- Failed: SPOONACULAR_API_KEY is not set in the environment or .env file

**Outcome (pass/fail):** Failed

**Logs/screenshots/evidence:**
<img width="1043" height="378" alt="image" src="https://github.com/user-attachments/assets/c5f4eb36-1728-4b63-b129-1796a4940044" />


**Next steps:** ensure that the spoonacular key is correctly included in the env file

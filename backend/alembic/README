Generic single-database configuration.

To update the database schema:
1. Make changes to the models in app/models/
2. Run: alembic revision --autogenerate -m "Description of changes"
3. Run: alembic upgrade head

Notes:
- Make sure that the DATABASE_URL in .env is set TO A **DATABASE AT THE CURRENT REVISION HEAD**!!
- If adding a new model, make sure to import it in env.py